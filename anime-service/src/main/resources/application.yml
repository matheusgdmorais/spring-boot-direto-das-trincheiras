spring:
  application:
    name: anime-service

  profiles:
    active: mysql
  threads:
    virtual:
      enabled: true

logging:
  level:
    academy.devdojo: debug

server:
  port: 8080
  servlet:
    context-path: /
  error:
    include-stacktrace: on_param
  tomcat:
    threads:
      max: 20

database:
  url: ${DB_URL:localhost}:${DB_PORT:3006}
  username: ${LOCAL_USERNAME:matheusgdmorais}
  password: ${LOCAL_PASSWORD:18213536}